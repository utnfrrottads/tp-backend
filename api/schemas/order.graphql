type Order {
	id: ID!
	lines: [OrderLine]
	createdAt: DateTime
	closedAt: DateTime
	table: Table!
	staff: Staff
}

type Query {
	orders: [Order!]!
	order(id: ID!): Order
}

type Mutation {
	createOrder: Order!
	deleteOrder(id: ID!): Int!
}
